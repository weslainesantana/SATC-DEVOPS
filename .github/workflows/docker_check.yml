name: Deploy to Kind Cluster

on:
  workflow_call:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build-and-analysis:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        with:
          args: >
            -Dsonar.projectKey=weslainesantana_SATC-DEVOPS
            -Dsonar.organization=weslainesantana
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.sources=.
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      # - name: Falhar se houver mais de 2 vulnerabilidades m̩dias
      #   run: |
      #     sh ./scripts/sonar_check.sh

#          if [ "$COUNT" -gt 2 ]; then
#            echo "Pipeline bloqueada: mais de 2 vulnerabilidades m̩dias encontradas."
#            exit 1
#          fi
        # env:
        #   SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        #   SONAR_HOST_URL: https://sonarcloud.io
        #   SONAR_PROJECT_KEY: weslainesantana_SATC-DEVOPS

#      - name: Log in to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}

#      - name: Build Docker image
#        run: docker build -t USUARIO/satc-devops:0.0.2 .

#      - name: Push Docker image
#        run: docker push docker.io/USUARIO/satc-devops:0.0.2

#      - name: Pull image
#        run: docker pull docker.io/USUARIO/satc-devops:0.0.2

#      - name: Load image into Kind
#        run: kind load docker-image docker.io/USUARIO/satc-devops:0.0.2 --name cluster-satc

#      - name: Deploy to cluster
#        run: kubectl apply -f deployment.yaml

      # - name: Run Trivy vulnerability scanner
      #   uses: aquasecurity/trivy-action@master
      #   with:
      #     scan-type: 'image'
      #     image-ref: 'docker.io/USUARIO/satc-devops:0.0.2'
      #     format: json
      #     output: trivy-results.json
      #     severity: MEDIUM,HIGH,CRITICAL

      # - name: Upload Trivy results as artifact
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: trivy-results.json
      #     path: trivy-results.json

      # - name: Analyze Trivy results
      #   run: |
      #     if [ -f trivy-results.json ]; then
      #       VULN_COUNT=$(jq '[.Results[].Vulnerabilities[] | select(.Severity == "MEDIUM")] | length' trivy-results.json)
      #       echo "Quantidade de vulnerabilidades m̩dias: $VULN_COUNT"

      #       if [ "$VULN_COUNT" -gt 2 ]; then
      #         echo "Pipeline bloqueada: mais de 2 vulnerabilidades m̩dias encontradas."
      #         exit 1
      #       fi
      #     else
      #       echo "Arquivo de resultados do Trivy ṇo encontrado."
      #       exit 1
      #     fi